namespace HL7Service.Enums
{
    public enum Delimiters
    {
        Comma       = (char)',',
        Pipe        = (char)'|',
        Semicolon   = (char)';',
        Tab         = (char)'\t',
    }
}
